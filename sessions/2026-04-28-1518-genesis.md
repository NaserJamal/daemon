# 2026-04-28 15:18 UTC — Agent-handoff scaffolding (P0)

**Phase:** P0 — Agent-handoff scaffolding
**Status:** landed

## Summary

Set up the repo so it can be improved incrementally via the handoff prompt
in `README.md`. Added the workflow scaffolding: `AGENTS.md` (conventions and
quality bar), `ROADMAP.md` (phased work queue), `sessions/` (breadcrumb log
with template and this seed), and `scripts/check.sh` (one-command quality
gate). No application code changed.

## Changes

- `AGENTS.md` — canonical guide for any agent working on the repo.
- `ROADMAP.md` — phase queue. Five "Now" phases (P1–P5), a `Later` backlog,
  and a `Done` log.
- `sessions/README.md`, `sessions/TEMPLATE.md` — breadcrumb conventions.
- `sessions/2026-04-28-1518-genesis.md` — this file.
- `scripts/check.sh` — runs pytest + ruff check + ruff format --check +
  mypy from the repo root, prints a one-line summary per stage.
- `README.md` — added an "Agent handoff workflow" section pointing to the
  scaffolding above.

## Quality gate

Output of `./scripts/check.sh` at end of session. These are the **floor** —
the next agent must not increase any of them, and `tests` and `format` must
stay green:

```
tests:     ok       (18 passed)
format:    ok       (ruff format applied once during this session)
lint:      5 errors (E741 in tools/edit.py x2; F401 in tests/test_tools.py x2; F401 in tools/__init__.py x1)
typecheck: 12 errors (mostly missing prompt_toolkit / pydantic stubs in cli/prompt.py and core/config.py)
```

P1 in `ROADMAP.md` is dedicated to driving lint and typecheck to zero.

## State of the application

Nothing about the runtime changed. `python -m daemon` and `daemon` both
still work; tests still pass. The only difference is process: there is now
a documented place for agents to start (`AGENTS.md`), a queue to pick from
(`ROADMAP.md`), a place to leave a trail (`sessions/`), and a single
verification command (`./scripts/check.sh`).

## Suggested next steps

1. **P1 — Lint & type cleanup.** Take the 5 ruff + 12 mypy errors to zero.
   Smallest, highest-leverage win because it tightens the quality gate for
   every phase that follows.
2. **P2 — Streaming responses.** Big UX upgrade with a contained blast
   radius (just `core/api.py` and `cli/main.py`).
3. **P3 — `/help` and slash-command discoverability.** Sets up the
   registry pattern that `CONTRIBUTING.md` already implies.

Pick whichever you can land cleanly in one session. Update `ROADMAP.md`'s
`Done` section with a link to your breadcrumb when finished.
